---
page_title: "Provider: Foundry"
description: |-
  The Foundry provider manages resources in the current Microsoft Foundry Agent Service.
---

# Foundry provider

The Foundry provider manages resources in the current Microsoft Foundry Agent Service.

## Example usage

```terraform
terraform {
  required_version = ">= 1.11.0"

  required_providers {
    foundry = {
      source = "oWretch/foundry"
    }
  }
}

provider "foundry" {
  account_name = "example"
  project_name = "example"
}
```

## Preview features

Some Foundry APIs are still in preview. The resources and data sources built on them are only usable after opting in by name through `enable_preview`:

```terraform
provider "foundry" {
  account_name = "example"
  project_name = "demo"

  enable_preview = ["evaluations", "memory_stores"]
}
```

Opt-in is per feature, so enabling one preview family does not enable the others. Using a preview resource or data source without listing its feature fails during `terraform validate` or `terraform plan`, before any API call is made.

| `enable_preview` value | Resources and data sources |
| --- | --- |
| `agent_endpoints` | The `agent_endpoint` attribute on agent resources |
| `draft_agents` | The `draft` attribute on agent resources |
| `evaluations` | `foundry_evaluation`, `foundry_evaluation_rule`, `foundry_evaluation_taxonomy`, `foundry_evaluator_version` |
| `external_agents` | `foundry_external_agent` |
| `memory_stores` | `foundry_memory_store` |
| `routines` | `foundry_routine` |
| `schedules` | `foundry_schedule` |
| `skills` | `foundry_skill` |
| `toolboxes` | `foundry_toolbox` |
| `toolbox_tools` | Preview tool types in the `tools` block of `foundry_toolbox` |

Preview tool types are named by the service with a `_preview` suffix, such as `fabric_iq_preview`. The suffix is part of the tool type, not a modifier the provider adds, and a preview type is not interchangeable with its non-preview counterpart: `a2a` and `a2a_preview` coexist and take different arguments. When a tool type reaches general availability the service publishes it under a new name, so configurations using the preview name must be updated at that point. Each preview tool in use is reported as a warning on every plan.

~> **Preview features are exempt from this provider's compatibility guarantees.** Their arguments, attributes, and behavior may change in any release, including a patch release, and the underlying preview API may be withdrawn by the service.

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `account_name` (String) Foundry account name. Can also be set with `FOUNDRY_ACCOUNT_NAME`.
- `api_key` (String, Sensitive) Foundry API key. Can also be set with `FOUNDRY_API_KEY`. API key authentication cannot be combined with Microsoft Entra ID authentication.
- `client_certificate_password` (String, Sensitive) Password for the service principal certificate. Can also be set with `ARM_CLIENT_CERTIFICATE_PASSWORD`.
- `client_certificate_path` (String) Path to a PEM or PKCS#12 service principal certificate. Can also be set with `ARM_CLIENT_CERTIFICATE_PATH`.
- `client_id` (String) Application or user-assigned managed identity client ID. Can also be set with `ARM_CLIENT_ID`.
- `client_secret` (String, Sensitive) Service principal client secret. Can also be set with `ARM_CLIENT_SECRET`.
- `enable_preview` (Set of String) Opts in to preview Foundry resources and data sources, individually by name (for example `["evaluations", "memory_stores"]`). Preview features may change their inputs, outputs, or behavior in any provider release without following semantic versioning. See the provider documentation for the list of preview feature names and the resources each one gates.
- `environment` (String) Azure environment. Supported values are `public`, `usgovernment`, and `china`. Defaults to `public` or `FOUNDRY_ENVIRONMENT` when set.
- `oidc_token_file_path` (String) Path to the workload identity token file. Can also be set with `ARM_OIDC_TOKEN_FILE_PATH` or `AZURE_FEDERATED_TOKEN_FILE`.
- `project_name` (String) Foundry project name. Can also be set with `FOUNDRY_PROJECT_NAME`.
- `tenant_id` (String) Microsoft Entra tenant ID. Can also be set with `ARM_TENANT_ID`.
- `use_cli` (Boolean) Use the signed-in Azure CLI account. Defaults to `true` when no other authentication mode is configured. Can also be set with `ARM_USE_CLI`.
- `use_msi` (Boolean) Use a system-assigned or user-assigned managed identity. Can also be set with `ARM_USE_MSI`.
- `use_oidc` (Boolean) Use workload identity federation. Can also be set with `ARM_USE_OIDC`.
